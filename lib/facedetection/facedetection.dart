//import 'package:flutter_face_detection/flutter_face_detection.dart';
//import 'dart:io' as IO;
//import 'package:flutter/painting.dart' as P;
//import 'package:flutter/material.dart' as M;
//
//class MyFaceDetector {
//  M.Image m = M.Image;
////  IO.File image;
//  MyFaceDetector(this.image});
//  var detector = FaceDetector(width: 280, height: 396);
//
//  Future<List<Face>> detectFace() async {
//    var bitmap = Bitmap.fromAssetImage(null);
//    Future<List<Face>> faces = detector.findFaces(bitmap);
//    for (var face in await faces) {
//      print(
//          "Found a face at (${face.midPoint.x}, ${face.midPoint.y}) with confidence ${face.confidence}");
//    }
//    return faces;
//  }
//}
